import { createContext } from "react";

const WrapperContext = createContext();
const HeaderContext = createContext();
const DestinationContext = createContext();

export { WrapperContext, HeaderContext, DestinationContext };